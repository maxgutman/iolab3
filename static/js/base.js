$(function() {
    // Define variables
    var tweets = $('#tweets'),
        tweetsList = $('#tweets-list');

    App = {
        // initialize event handler: clicks and keypress
        init: function() {

        },
        // Do something
        sampleFunction: function(user) {

        },
        // General method for rendering -- may be unused
        render: function() {

        }
     };
     /* initialize and render for the first time */
     App.init();
     App.render();

});